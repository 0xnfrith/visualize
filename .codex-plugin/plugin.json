{
  "name": "visualize",
  "version": "0.3.2",
  "description": "Diagram-rendering canvas. Codex draws via D2 / raw SVG / image URLs; operator riffs on a tldraw surface. Requires `bun` and `d2` on PATH.",
  "author": {
    "name": "Nicholas Frith"
  },
  "repository": "https://github.com/nfrith/visualize",
  "license": "Apache-2.0",
  "keywords": [
    "canvas",
    "d2",
    "tldraw",
    "mcp",
    "diagrams"
  ],
  "skills": "./skills/",
  "mcpServers": "./.mcp.codex.json",
  "interface": {
    "displayName": "Visualize",
    "shortDescription": "Diagram-rendering canvas (D2, raw SVG, image-by-URL) on a tldraw surface.",
    "longDescription": "Gives the agent a diagram-rendering canvas. The agent generates D2 (or raw SVG, or an image URL); the plugin renders it server-side and pushes the result onto a tldraw surface the operator can pan, zoom, drag, and annotate. Requires `bun` and `d2` on PATH.",
    "developerName": "Nicholas Frith",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Write"
    ]
  }
}
